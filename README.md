# 复制档案 Clipboard Atlas

Windows desktop clipboard history dashboard built with Electron and React.

## Run

```bash
npm install
npm run dev
```

The app polls the system clipboard, stores records locally for 48 hours, and supports text and image clipboard content. A record click restores it to the clipboard and moves it to the top.
